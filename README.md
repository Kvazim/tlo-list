TLO List — Test React Application

Тестовое одностраничное приложение для отображения светофорных объектов в виде списка и карты с синхронизацией состояния между ними.

🔗 Демо: [(Vercel link)](https://tlo-list.vercel.app)
🔗 Репозиторий: [(GitHub link)](https://github.com/Kvazim/tlo-list)

🚀 Функциональность
📋 Список объектов

Отображение номера и адреса объекта

Визуальное различие объектов по статусу (mode)

Фильтрация:

по номеру / адресу

по статусу объекта

Клик по элементу списка → центрирование карты на объекте

🗺 Карта

Реализация на leaflet / react-leaflet

Отображение объектов с разным стилем в зависимости от статуса

Tooltip с информацией об объекте

Синхронизация с фильтрами списка

🔄 Взаимодействие

Фильтрация списка автоматически фильтрует объекты на карте

Выбор объекта в списке позиционирует карту на выбранной точке

🧠 Выбор стека и технологий
⚛ React 19

Современная версия React

Функциональные компоненты и хуки

Предсказуемая модель обновления UI

⚡ Vite

Быстрый dev-сервер

Мгновенный HMR

Быстрая сборка для production

🗺 Leaflet + React-Leaflet

Проверенное решение для карт

Хорошая производительность

Удобная интеграция с React

🎨 Tailwind CSS

Utility-first подход

Минимальный CSS-бандл

Быстрое прототипирование и поддержка

🧩 @tanstack/react-virtual

Виртуализация списка

Поддержка динамической высоты элементов

Значительное снижение нагрузки при большом количестве объектов

⚙️ Оптимизации
🔹 Виртуализация списка

Для списков с большим количеством элементов используется @tanstack/react-virtual:

Рендерятся только видимые элементы

Поддержка динамической высоты строк

Используется:

estimateSize — для первичного расчёта

measureElement — для точного измерения высоты

getItemKey — для корректной работы при сортировке и фильтрации

Это предотвращает:

лишние перерисовки

“слипание” элементов при изменении порядка данных

🔹 Синхронизация списка и карты

Все фильтры применяются к единому источнику данных

Карта и список используют одно состояние

Исключены рассинхронизации UI

🔹 Минимизация лишних ререндеров

Чистые компоненты

Отсутствие избыточного состояния

Простая и прозрачная архитектура

🧪 Качество кода

TypeScript — строгая типизация

ESLint — контроль качества кода

Явное разделение:

UI

логики

типов

#Требования:
1. Приложение должно отображать светофорные объекты в виде:
списка;
объектов на карте.
2. Требования к списку:
для каждого объекта отображается по крайней мере его номер и адрес;
в зависимости от статуса (поле mode) объекты в списке должны отличаться;
возможность фильтрации списка по номеру/адресу;
возможность фильтрации списка по статусу объекта (поле mode).
3. Требования к картографическому отображению:
использовать leaflet/react-leaflet;
в зависимости от статуса (поле mode) объекты на карте должны отличаться;
при клике на объект отображается тултип с информацией о выбранном объекте;
4. Требования по взаимодействию списка и карты:
при фильтрации объектов в списке, объекты на карте также фильтруются;
при клике на объекте в списке на карте происходит позиционирование на объекте.
